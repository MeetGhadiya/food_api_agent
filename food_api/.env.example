# FoodieExpress FastAPI Backend - Environment Variables Template
# INSTRUCTIONS: 
# 1. Copy this file to `.env` in the same directory
# 2. Replace ALL placeholder values with your actual credentials
# 3. NEVER commit the actual .env file to version control

# ==================== JWT AUTHENTICATION ====================
# Generate a secure secret key using: openssl rand -hex 32
# This key is used to sign JWT tokens - keep it SECRET!
SECRET_KEY="your_newly_generated_32_byte_hex_secret_here"

# ==================== DATABASE CONFIGURATION ====================
# MongoDB Atlas connection string
# CRITICAL: Replace with your actual MongoDB credentials
# Format: mongodb+srv://username:password@cluster.mongodb.net/?options
# IMPORTANT: After copying this file, immediately rotate your MongoDB password in Atlas!
MONGO_DATABASE_URL="mongodb+srv://foodapi_user:YOUR_NEW_ROTATED_PASSWORD@foodapicluster.6z9sntm.mongodb.net/?retryWrites=true&w=majority&appName=FoodAPICluster"

# ==================== CORS CONFIGURATION ====================
# Comma-separated list of allowed origins for CORS
# Example: "http://localhost:5173,http://localhost:3000,https://yourdomain.com"
ALLOWED_ORIGINS="http://localhost:5173,http://localhost:5174,http://localhost:3000"

# ==================== OPTIONAL: REDIS SESSION STORE ====================
# Uncomment and configure if using Redis for session management
# REDIS_HOST="localhost"
# REDIS_PORT="6379"
# REDIS_PASSWORD=""
# REDIS_DB="0"
