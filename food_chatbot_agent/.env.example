# FoodieExpress AI Chatbot Agent - Environment Variables Template
# INSTRUCTIONS: 
# 1. Copy this file to `.env` in the same directory
# 2. Replace ALL placeholder values with your actual credentials
# 3. NEVER commit the actual .env file to version control

# ==================== GOOGLE GEMINI AI ====================
# Get your API key from: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY="your_google_gemini_api_key_here"

# ==================== FASTAPI BACKEND ====================
# URL of the FastAPI backend service
FASTAPI_BASE_URL="http://localhost:8000"

# Agent Server Port
AGENT_PORT="5000"

# ==================== REDIS SESSION STORE (NEW IN V3.0) ====================
# 🚀 ARCHITECTURE UPGRADE: Distributed session management with Redis
# Redis enables horizontal scaling and session persistence across restarts

# Enable/Disable Redis (set to 'false' to use in-memory fallback for development)
REDIS_ENABLED="true"

# Redis Connection Settings
REDIS_HOST="localhost"           # Use 'localhost' for Docker/local Redis
REDIS_PORT="6379"                # Default Redis port
REDIS_DB="0"                     # Database index (0-15)
REDIS_PASSWORD=""                # Leave empty for local development, SET IN PRODUCTION!

# Session TTL (Time-To-Live) Configuration
SESSION_TTL="3600"               # Chat session expiry: 1 hour (3600 seconds)
PENDING_ORDER_TTL="600"          # Pending order expiry: 10 minutes (600 seconds)

# Development Mode Settings
REDIS_FALLBACK_ENABLED="true"    # Fallback to in-memory if Redis fails (dev only)
                                 # Set to 'false' in production for fail-fast behavior
